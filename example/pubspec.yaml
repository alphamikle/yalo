name: example
description: Example for yalo package
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  device_preview: ^1.1.0
  intl: ^0.17.0
  yalo:
    path: ../
  yalo_locale:
    path: ./.yalo_locale
#  yalo_assets:
#    path: ./.yalo_assets

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/